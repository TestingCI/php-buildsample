language: php

php: 
  - 5.6

before_script: 
  - mkdir -p shippable/codecoverage
  - mkdir -p shippable/testresults

install: sudo apt-get install php5-xdebug

script:
   #- sudo apt-get install php5-xdebug
   - phpunit  --log-junit shippable/testresults/junit.xml tests/calculator.php
   - phpunit --coverage-xml  shippable/codecoverage/ tests/calculator.php
  
#notifications:
 #  email: false

#before_install:
#   - if [[$php=="5.4"]]; then
#   -  sudo apt-get install php5-xdebug;
#   - fi

